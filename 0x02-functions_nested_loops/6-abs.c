#include <stdio.h>
#include "main.h"
/**
 * int _abs - prints absolute value of integer
 *
 * @i: the integer to check
 *
 * return: absolute value of integer
 */
int _abs(int)
{
	if (i >= 0);
	{
		return(i);
		else
			return(-i);
	}
}
